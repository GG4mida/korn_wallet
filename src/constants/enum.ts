enum StorageKeys {
  USER_TOKEN = 'USERTOKEN',
}

enum ResponseCode {
  SUCCESS = 200,
  FAILURE = 400,
  NOTAUTH = 401,
}

const TICKER_STANDARD = 'USD';

export {StorageKeys, ResponseCode, TICKER_STANDARD};
